# InfinityXOS Credential Manager (LOCAL)
Path: C:\Users\JARVIS\AppData\Local\InfinityXOne\CredentialManager

This folder is the local credential substrate for InfinityXOS.
Secrets are stored OUTSIDE git and OUTSIDE the repo.

Recommended secret keys:
- github_pat
- openai_api_key
- gcp_sa_json_path (store PATH only, not the key contents)