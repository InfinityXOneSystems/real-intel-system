name: Deploy Control Plane

on:
  push:
    branches: [master]
    paths:
      - 'control-plane/**'
      - 'helm/control-plane/**'
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./control-plane
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/control-plane:latest
            ghcr.io/${{ github.repository_owner }}/control-plane:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    # Uncomment when cluster credentials are available
    # steps:
    #   - uses: actions/checkout@v4
    #   - name: Configure kubectl
    #     run: |
    #       echo "${{ secrets.KUBECONFIG }}" | base64 -d > kubeconfig
    #       export KUBECONFIG=kubeconfig
    #   - name: Deploy via Helm
    #     run: |
    #       helm upgrade --install control-plane ./helm/control-plane \
    #         -n control-plane \
    #         -f ./helm/control-plane/values.production.yaml \
    #         --set image.tag=${{ github.sha }} \
    #         --wait
    #   - name: Verify deployment
    #     run: |
    #       kubectl -n control-plane rollout status deploy/control-plane
    #       kubectl -n control-plane get pods
    steps:
      - name: Placeholder
        run: echo "Cluster credentials not configured. Add KUBECONFIG secret to enable auto-deploy."
