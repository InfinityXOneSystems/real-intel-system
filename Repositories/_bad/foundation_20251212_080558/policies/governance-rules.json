{
  "rules": [
    {
      "id": "no-secrets-in-code",
      "description": "No secrets or credentials may be committed to the repository. All secrets must be managed via environment variables or secret managers.",
      "enforced": true
    },
    {
      "id": "require-ci-pass",
      "description": "All PRs must pass CI validation (lint, type-check, tests, secret scan, audit) before merge.",
      "enforced": true
    },
    {
      "id": "dependabot-enabled",
      "description": "Dependabot must be enabled for all dependencies.",
      "enforced": true
    },
    {
      "id": "sop-versioning",
      "description": "All SOPs must be versioned and updated on change.",
      "enforced": true
    }
  ]
}
