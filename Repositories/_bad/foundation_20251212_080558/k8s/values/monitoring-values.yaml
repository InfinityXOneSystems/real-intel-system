grafana:
  enabled: true
  persistence:
    enabled: false
prometheus:
  prometheusSpec:
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        cpu: 500m
        memory: 512Mi
    retention: 24h
grafana:
  enabled: true
  adminPassword: "VAULT:secret/data/grafana#admin_password"
prometheus:
  prometheusSpec:
    retention: 15d
    resources:
      requests:
        cpu: 200m
        memory: 512Mi
